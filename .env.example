# ============================================================
# Kone The Lay Myar Digital (KTLM) - Environment Variables
# ============================================================
# Copy this file to .env.local and fill in real values.
# Never commit .env.local or any file containing real secrets.
# ============================================================

# MongoDB connection string (required)
# Example: mongodb+srv://USER:PASSWORD@cluster.mongodb.net/DATABASE?retryWrites=true&w=majority
MONGODB_URI=

# JWT secret for session signing (required in production)
# Use a long random string; e.g. openssl rand -base64 32
JWT_SECRET=

# Optional: base path when deploying under a subpath (e.g. /myapp)
# Leave empty for root deployment
# NEXT_PUBLIC_BASE_PATH=

# NODE_ENV is set automatically by Next.js (development | production)
# Do not override unless needed for testing
# NODE_ENV=development
